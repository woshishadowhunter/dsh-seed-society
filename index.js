// dsh-yogacara-society entry stub. The plugin is configuration-only: it
// patches existing entries and inserts the MCP society bridge via
// cordis.patch.yml; this module is never imported by the loader.
export {};
